<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--从底部弹出-->
    <style name="Animation.Transit.Bottom" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_transit_in_from_bottom</item>
        <item name="android:windowExitAnimation">@anim/anim_transit_out_to_bottom</item>
    </style>

    <style name="BottomDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/Animation.Transit.Bottom</item>
        <!--        <item name="statusBarBackground">@android:color/transparent</item>-->
        <!--        <item name="statusBarForeground">@android:color/transparent</item>-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="test_text" parent="android:Widget.TextView">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">top|center</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_gravity">center</item>
    </style>
</resources>